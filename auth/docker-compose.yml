version: "3.9"
services:
  application:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
      - "8444:8444"
    environment:
      - packageVersion
      - gitToken
      - environment
      - domain
      - audience
      - realm
      - secret
      - keystore
      - keyAlias
      - keyStorePassword
      - privateKeyPassword