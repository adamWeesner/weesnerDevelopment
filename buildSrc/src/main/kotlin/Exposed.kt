object Exposed {
    private const val base = "org.jetbrains.exposed:exposed-"

    const val version = "0.36.1"

    const val core = "${base}core:$version"
    const val jdbc = "${base}jdbc:$version"
    const val dao = "${base}dao:$version"
}
