object KodeIn {
    private const val base = "org.kodein.di:kodein-di-"

    const val version = "6.5.5"

    const val core = "${base}generic-jvm:$version"
    const val ktorServer = "${base}framework-ktor-server-jvm:7.12.0"
}
