package com.weesnerdevelopment.routes

#routeImports
import io.ktor.auth.*
import io.ktor.routing.*
import org.kodein.di.generic.instance
import org.kodein.di.ktor.kodein

fun Routing.#titleRoutes() {
    #instances
    #routes
}
